// https://github.com/fedm4/js-course

// Ejercicio: 

/* ------------------------------- 
*/

const calcular = () => {
    let num = Number(prompt("Introduzca un número para calcular su cuadrado"));
    let resultado = (num * num); // square(num);
    alert(`El resultado es: ${resultado}`);
}
calcular();